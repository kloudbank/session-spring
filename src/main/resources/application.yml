server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: "*"

spring:
  config:
    import: application-redis.yml
  application:
    name: session-spring-local
  # redis:
  #   host: 127.0.0.1
  #   port: 7000